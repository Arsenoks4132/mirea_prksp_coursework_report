#import "../../const.typ"

= ПРОЕКТИРОВАНИЕ КЛИЕНТ-СЕРВЕРНОЙ АРХИТЕКТУРЫ

== Обоснование выбора клиент-серверной архитектуры

Разрабатываемое приложение управления рабочими процессами по своей природе является многопользовательской системой: в ней одновременно работают сотрудники и руководители, данные должны быть централизованы и доступны с любого устройства, а логика разграничения доступа должна быть надёжно защищена на стороне сервера. Именно эти требования обусловили выбор клиент-серверной архитектуры.

Клиент-серверная архитектура предполагает явное разделение системы на две части: клиент, который инициирует запросы и отображает результат, и сервер, который обрабатывает запросы, выполняет бизнес-логику и управляет данными @martin2021. В рамках данного приложения роль клиента выполняет веб-браузер пользователя, а роль сервера — набор сервисов, развёрнутых в контейнерах.

По сравнению с альтернативами клиент-серверная архитектура имеет ряд ключевых преимуществ для данной задачи:

- централизованное хранение данных исключает дублирование и рассинхронизацию сведений о задачах и сотрудниках;
- разграничение доступа реализуется на стороне сервера, что исключает возможность его обхода на клиенте;
- обновление бизнес-логики и данных происходит в одном месте и немедленно становится доступным всем пользователям;
- клиент не требует установки — достаточно браузера, что снижает порог входа для пользователей.

=== Трёхуровневая модель архитектуры

Разрабатываемое приложение построено по трёхуровневой (three-tier) клиент-серверной модели, которая разделяет систему на три самостоятельных уровня @martin2021:

*Уровень представления (Presentation Tier)* — клиентская часть, с которой взаимодействует пользователь. В данном проекте клиентом является стандартный веб-браузер. Он отправляет HTTP-запросы и отображает HTML-страницы, сформированные сервером. Никакой бизнес-логики на клиенте нет: весь рендеринг и вся обработка данных выполняются на стороне сервера.

*Уровень логики приложения (Application Tier)* — серверная часть, реализованная на Django. На этом уровне выполняется аутентификация пользователей, проверка прав доступа, обработка данных форм, выполнение CRUD-операций через ORM, агрегирование статистики и генерация HTML-ответов. Gunicorn обеспечивает WSGI-интерфейс между веб-сервером и Django. Nginx принимает внешние запросы, отдаёт статические файлы напрямую и проксирует динамические запросы к серверу приложений. Traefik выступает точкой входа и балансировщиком нагрузки.

*Уровень данных (Data Tier)* — серверы данных. PostgreSQL хранит все данные системы: пользователей, категории задач, задачи. Redis обеспечивает кэширование сформированных страниц для снижения нагрузки на сервер приложений.

=== UML-описание архитектуры

Общая схема взаимодействия компонентов в клиент-серверной архитектуре приложения представлена на рисунке @client-server-architecture. Диаграмма отражает маршрут HTTP-запроса от браузера пользователя через балансировщик нагрузки, веб-сервер и сервер приложений до базы данных и обратно.

#figure(
  image("../../images/18_client_server_diagram.png", width: 95%),
  caption: [Структурная схема клиент-серверной архитектуры приложения],
) <client-server-architecture>

/*
  ПЛЕЙСХОЛДЕР — компонентная диаграмма
  Файл: images/component_diagram.png
  Источник: diagrams/component_diagram.puml
  Описание: UML-диаграмма компонентов системы.
  Показывает: Browser → Traefik → Nginx → Gunicorn → Django (Router, Views,
  Forms, ORM, Templates, Auth, Cache) → PostgreSQL / Redis.
  Как получить: отрендерить diagrams/component_diagram.puml через PlantUML
  и сохранить результат как images/component_diagram.png
*/
#figure(
  rect(width: 95%, height: 9cm, stroke: (paint: black, dash: "dashed"))[
    #align(center + horizon)[
      *[ПЛЕЙСХОЛДЕР]*\
      Компонентная UML-диаграмма\
      Источник: `diagrams/component_diagram.puml`\
      Отрендерить и сохранить как `images/component_diagram.png`
    ]
  ],
  caption: [Компонентная диаграмма приложения TaskAndTimes],
) <component-diagram>

Диаграмма вариантов использования системы представлена на рисунке @use-case-diagram. Она показывает действия, доступные каждому актору: сотруднику, руководителю и администратору.

/*
  ПЛЕЙСХОЛДЕР — диаграмма вариантов использования
  Файл: images/use_case_diagram.png
  Источник: diagrams/use_case.puml
  Описание: UML use-case диаграмма.
  Акторы: Сотрудник, Руководитель, Администратор.
  Сотрудник: регистрация, вход, просмотр профиля, добавление/редактирование/
  удаление своих задач, редактирование профиля, смена пароля, выход.
  Руководитель: вход, статистика, задачи сотрудника, управление категориями,
  редактирование/удаление любых задач, выход.
  Как получить: отрендерить diagrams/use_case.puml через PlantUML
  и сохранить результат как images/use_case_diagram.png
*/
#figure(
  rect(width: 95%, height: 12cm, stroke: (paint: black, dash: "dashed"))[
    #align(center + horizon)[
      *[ПЛЕЙСХОЛДЕР]*\
      Use-case UML-диаграмма\
      Источник: `diagrams/use_case.puml`\
      Отрендерить и сохранить как `images/use_case_diagram.png`
    ]
  ],
  caption: [Диаграмма вариантов использования системы],
) <use-case-diagram>

== Описание клиентской части

Клиентская часть приложения реализована на основе серверного рендеринга (Server-Side Rendering): сервер формирует готовые HTML-страницы и передаёт их в браузер пользователя. Такой подход упрощает разработку и обеспечивает корректную работу в любом современном браузере без необходимости установки дополнительного программного обеспечения на стороне клиента @web-dev-tuzovskiy.

Клиентская часть построена с использованием следующих технологий:

- *HTML* — разметка веб-страниц. Шаблоны Django (`*.html`) содержат структуру страниц и подставляют данные, переданные из представлений через контекст.
- *CSS* — стилизация интерфейса. Единый файл `static/Tasks/css/styles.css` задаёт оформление всех компонентов: форм, карточек задач, профиля, таблиц статистики.
- *Шаблонный движок Django* — обеспечивает наследование шаблонов (`base.html`), пользовательские теги (`post_form`), условные конструкции и циклы в шаблонах.

Клиентская часть включает следующие основные страницы:

- главная страница с навигацией по точкам входа;
- страница авторизации сотрудника и руководителя;
- страница регистрации;
- страница профиля сотрудника (статистика + форма добавления задачи + список задач);
- страница редактирования профиля и смены пароля;
- страница статистики по всем сотрудникам (для руководителя);
- страница задач конкретного сотрудника (для руководителя);
- страницы редактирования и удаления задачи;
- страницы управления категориями (список, создание, редактирование, удаление);
- страницы ошибок 403 и 404.

Взаимодействие браузера с сервером осуществляется через стандартные HTML-формы: GET-запрос для загрузки страницы, POST-запрос для отправки данных формы. CSRF-защита реализована встроенными средствами Django.

== Выбор программного стека

=== Серверный фреймворк

Для разработки серверной части рассмотрены Python-фреймворки Django, Flask и FastAPI.

Flask представляет собой легковесный фреймворк с высокой степенью свободы. Однако при разработке приложения с регистрацией, ролевым доступом, административным интерфейсом и ORM разработчик вынужден самостоятельно подбирать и интегрировать большое число дополнительных библиотек @flask-docs.

FastAPI ориентирован на разработку API с высокой производительностью и встроенной валидацией данных. Для данного проекта, в котором требуется серверный рендеринг HTML-страниц, форм и шаблонов, FastAPI не является наиболее естественным выбором @fastapi-docs.

Django — высокоуровневый фреймворк, уже содержащий встроенные механизмы для всех ключевых задач: ORM, маршрутизацию, формы, аутентификацию, административную панель и систему шаблонов @mele2023. В рамках Django применяется паттерн MVT (Model–View–Template), обеспечивающий логичное разделение приложения на данные, обработку запросов и отображение.

Сравнение фреймворков представлено в таблице @frameworks-table.

#figure(
  table(
    columns: (2.2fr, 1.6fr, 1.6fr, 1.6fr),
    [Критерий], [Django], [Flask], [FastAPI],
    [Встроенная ORM], [Да], [Нет], [Нет],
    [Шаблоны и формы], [Да], [Частично], [Нет],
    [Встроенная аутентификация], [Да], [Нет], [Нет],
    [Административная панель], [Да], [Нет], [Нет],
    [Паттерн MVT/MVC], [MVT], [Нет], [Нет],
    [Применимость для проекта], [Высокая], [Средняя], [Низкая],
  ),
  caption: [Сравнение серверных фреймворков],
) <frameworks-table>

По результатам сравнения для разработки выбран Django.

=== СУБД

Разрабатываемое приложение хранит структурированные данные о пользователях, категориях и задачах с отношениями между сущностями. Рассмотрены PostgreSQL и MySQL.

PostgreSQL обеспечивает строгий контроль целостности данных, полную поддержку транзакций и эффективное выполнение агрегатных запросов, необходимых для расчёта статистики @pipeops2024. Кроме того, PostgreSQL является рекомендованной СУБД для Django в продуктивных средах @postgresql-docs.

MySQL также широко применяется в веб-разработке, однако PostgreSQL предпочтительнее для задач, требующих сложных аналитических запросов и высокой надёжности. По совокупности критериев для проекта выбрана PostgreSQL.

=== Кэш и вспомогательные сервисы

Redis выбран в качестве кэш-хранилища. Он хранит данные в оперативной памяти, обеспечивает высокую скорость доступа и хорошо интегрируется с Django через встроенный кэш-бэкенд @redis-docs. Кэширование применяется для страниц профиля, статистики и списка задач.

Nginx выбран в качестве веб-сервера для отдачи статических и медиафайлов, а также обратного прокси для динамических запросов @chihana2018. Traefik используется как балансировщик нагрузки и входная точка, удобно работающая в контейнерной среде Docker @johansson2022.

Docker с Docker Compose обеспечивает воспроизводимую сборку и запуск всех сервисов приложения в изолированных контейнерах @godzuras2023.

=== Итоговый стек технологий

Итоговый выбор технологий представлен в таблице @stack-table.

#figure(
  table(
    columns: (1.5fr, 2fr, 2.5fr),
    [Компонент], [Технология], [Обоснование выбора],
    [Backend-фреймворк], [Django 5.1], [Встроенный ORM, аутентификация, шаблоны, MVT],
    [СУБД], [PostgreSQL 17], [Целостность данных, агрегатные запросы],
    [Кэш], [Redis], [Быстрый доступ, интеграция с Django],
    [Веб-сервер], [Nginx 1.27], [Отдача статики, обратный прокси],
    [Балансировщик], [Traefik v3.2], [Маршрутизация, Docker-интеграция],
    [WSGI-сервер], [Gunicorn], [Продуктивный запуск Django],
    [Контейнеризация], [Docker + Compose], [Воспроизводимое развёртывание],
    [Язык программирования], [Python 3.12], [Поддержка Django, экосистема],
    [Клиентская часть], [HTML + CSS + Django Templates], [Серверный рендеринг],
  ),
  caption: [Итоговый стек технологий],
) <stack-table>

== Выбор системы контроля версий

Для хранения исходного кода и отслеживания истории изменений выбрана система контроля версий Git как наиболее распространённый инструмент в современной разработке программного обеспечения @docker-docs. В качестве удалённого репозитория используется платформа GitHub.

Применение Git и GitHub обеспечивает следующие возможности:

- полная история изменений кода с содержательными сообщениями коммитов;
- возможность ветвления и слияния для организации работы;
- открытый доступ к исходному коду для демонстрации преподавателю;
- интеграция с системами CI/CD для автоматизированного тестирования и развёртывания.

Исходный код проекта размещён в репозитории по адресу: `https://github.com/Arsenoks4132/TaskMetrics`.

== Выбор облачной платформы

Для развёртывания приложения в продуктивной среде выбрана платформа Yandex Cloud — отечественный облачный провайдер с развитой инфраструктурой. В качестве модели развёртывания выбрана виртуальная машина (Compute Cloud), что обеспечивает полный контроль над окружением и позволяет использовать Docker Compose напрямую без ограничений PaaS-платформ.

Обоснование выбора Yandex Cloud:

- доступность из российских сетей без ограничений;
- поддержка Ubuntu-образов с возможностью установки Docker;
- оплата по факту использования ресурсов;
- простота управления через веб-консоль.

Приложение развёртывается на ВМ с запуском `docker compose up -d --build`, что обеспечивает одновременный старт всех пяти сервисов: Django (×3), Nginx (×3), PostgreSQL, Redis и Traefik. URL задеплоенного приложения будет приведён после завершения развёртывания.

== Выводы по главе

В данной главе обоснован выбор клиент-серверной архитектуры для приложения управления рабочими процессами. Спроектирована трёхуровневая модель системы: уровень представления (браузер), уровень логики приложения (Django + Gunicorn + Nginx + Traefik) и уровень данных (PostgreSQL + Redis). Архитектура описана с помощью UML-диаграмм: структурной схемы взаимодействия компонентов, компонентной диаграммы и диаграммы вариантов использования.

Определён программный стек для реализации fullstack CRUD-приложения, обоснован выбор каждой из технологий. В качестве системы контроля версий выбран Git с хостингом на GitHub; для облачного развёртывания — виртуальная машина Yandex Cloud.
